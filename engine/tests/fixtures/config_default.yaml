strategy:
  min_edge_threshold: 0.05
  max_entry_price: 0.15
  min_exit_price: 0.45
  uncertainty_base_f: 2.5
  uncertainty_per_day_f: 0.5
  fee_estimate: 0.02
  slippage_estimate: 0.01

risk:
  max_position_size_usd: 5.00
  max_trades_per_run: 3
  max_total_exposure_usd: 25.00
  max_per_city_exposure_usd: 10.00
  max_daily_loss_usd: 10.00
  cooldown_minutes: 30
  min_hours_to_resolution: 6.0
  slippage_ceiling: 0.05

execution:
  mode: dry-run
  adapter: dry-run
